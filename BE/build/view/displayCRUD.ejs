<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>get CRUD</title>

    <style>
        table, th, td {
          border:1px solid black;
          border-collapse : collapse;
        }

        .button-edit{
            background-color: bisque;
            padding:10px 25px;
            margin :5px;
            display: inline-block;
        }

        .button-delete{
            background-color: red;
            padding:10px 25px;
            margin:5px;
            display: inline-block;
        }
        
        </style>

</head>
<body>

    <table style="width:100%">
        <tr>
            <td>Email</td>
            <td>First Name</td>
            <td>Last Name</td>
            <td>Adress</td>
            <td>Action</td>
        </tr>
        <%
            for(let i = 0 ; i < dataTable.length ; i++){%>
                <tr>
                    <td><%=dataTable[i].email%></td>
                    <td><%=dataTable[i].firstName%></td>
                    <td><%=dataTable[i].lastName%></td>
                    <td><%=dataTable[i].address%></td>
                    <td>
                        <a href ="/edit-crud?id=<%= dataTable[i].id%>" type = "button" class = "button-edit">Edit</a>
                        <a href ="/delete-crud?id=<%= dataTable[i].id%>" type = "button" class = "button-delete">Delete</a>
                    </td>
                </tr>
           <% }%>

        
      </table>
    
</body>
</html>